---
import { isHidden } from '~/lib/launch';
export const prerender = !isHidden('/about');
if (isHidden('/about')) return Astro.redirect('/fr', 302);
import AboutView from '~/components/views/AboutView.astro';
---
<AboutView locale="fr" />
