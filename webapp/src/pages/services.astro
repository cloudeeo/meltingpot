---
import { isHidden } from '~/lib/launch';
export const prerender = !isHidden('/services');
if (isHidden('/services')) return Astro.redirect('/', 302);
import ServicesView from '~/components/views/ServicesView.astro';
---
<ServicesView locale="en" />
